import React from "react";
import { AppBar, Toolbar, Typography, Button, Box } from "@mui/material";
import { Link } from "react-router-dom";
import { ShoppingCart, ListAlt, Login } from "@mui/icons-material";

function Navbar() {
  return (
    <AppBar
      position="sticky"
      sx={{
        backdropFilter: "blur(10px)",
        bgcolor: "rgba(13,71,161,0.9)",
      }}
    >
      <Toolbar>
        <Typography
          variant="h6"
          component={Link}
          to="/"
          sx={{ flexGrow: 1, textDecoration: "none", color: "inherit", fontWeight: 600 }}
        >
          🛍️ Mi Tienda
        </Typography>
        <Box>
          <Button color="inherit" component={Link} to="/carrito" startIcon={<ShoppingCart />}>
            Carrito
          </Button>
          <Button color="inherit" component={Link} to="/pedidos" startIcon={<ListAlt />}>
            Pedidos
          </Button>
          <Button color="inherit" component={Link} to="/login" startIcon={<Login />}>
            Login
          </Button>
        </Box>
      </Toolbar>
    </AppBar>
  );
}

export default Navbar;
